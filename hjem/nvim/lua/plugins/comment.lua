return {
	"numToStr/Comment.nvim",

	keys = {
		{ mode = "v", "gb", "Comment toggle blockwise (visual)" },
		{ mode = "v", "gc", "Comment toggle linewise (visual)" },
		{ "gbc", "Comment toggle current block" },
		{ "gb", "Comment toggle blockwise" },
		{ "gcc", "Comment toggle linewise" },
	},
	config = true,
}
